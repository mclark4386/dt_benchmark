package actions

import (
	"cpsg-git.mattclark.guru/highlands/dt_benchmark/models"
	"github.com/gobuffalo/buffalo"
	"github.com/gobuffalo/pop"
	"github.com/pkg/errors"
)

// This file is generated by Buffalo. It offers a basic structure for
// adding, editing and deleting a page. If your model is more
// complex or you need more than the basic implementation you need to
// edit this file.

// Following naming logic is implemented in Buffalo:
// Model: Singular (Benchmark)
// DB Table: Plural (benchmarks)
// Resource: Plural (Benchmarks)
// Path: Plural (/benchmarks)
// View Template Folder: Plural (/templates/benchmarks/)

// BenchmarksResource is the resource for the Benchmark model
type BenchmarksResource struct {
	buffalo.Resource
}

// List gets all Benchmarks. This function is mapped to the path
// GET /benchmarks
func (v BenchmarksResource) List(c buffalo.Context) error {
	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	benchmarks := &models.Benchmarks{}

	// Paginate results. Params "page" and "per_page" control pagination.
	// Default values are "page=1" and "per_page=20".
	q := tx.PaginateFromParams(c.Params())

	// Retrieve all Benchmarks from the DB
	if err := q.All(benchmarks); err != nil {
		return errors.WithStack(err)
	}

	// Add the paginator to the context so it can be used in the template.
	c.Set("pagination", q.Paginator)

	return c.Render(200, r.Auto(c, benchmarks))
}

// Show gets the data for one Benchmark. This function is mapped to
// the path GET /benchmarks/{benchmark_id}
func (v BenchmarksResource) Show(c buffalo.Context) error {
	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	// Allocate an empty Benchmark
	benchmark := &models.Benchmark{}

	// To find the Benchmark the parameter benchmark_id is used.
	if err := tx.Find(benchmark, c.Param("benchmark_id")); err != nil {
		return c.Error(404, err)
	}

	return c.Render(200, r.Auto(c, benchmark))
}

// New renders the form for creating a new Benchmark.
// This function is mapped to the path GET /benchmarks/new
func (v BenchmarksResource) New(c buffalo.Context) error {
	return c.Render(200, r.Auto(c, &models.Benchmark{}))
}

// Create adds a Benchmark to the DB. This function is mapped to the
// path POST /benchmarks
func (v BenchmarksResource) Create(c buffalo.Context) error {
	// Allocate an empty Benchmark
	benchmark := &models.Benchmark{}

	// Bind benchmark to the html form elements
	if err := c.Bind(benchmark); err != nil {
		return errors.WithStack(err)
	}

	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	// Validate the data from the html form
	verrs, err := tx.ValidateAndCreate(benchmark)
	if err != nil {
		return errors.WithStack(err)
	}

	if verrs.HasAny() {
		// Make the errors available inside the html template
		c.Set("errors", verrs)

		// Render again the new.html template that the user can
		// correct the input.
		return c.Render(422, r.Auto(c, benchmark))
	}

	// If there are no errors set a success message
	c.Flash().Add("success", "Benchmark was created successfully")

	// and redirect to the benchmarks index page
	return c.Render(201, r.Auto(c, benchmark))
}

// Edit renders a edit form for a Benchmark. This function is
// mapped to the path GET /benchmarks/{benchmark_id}/edit
func (v BenchmarksResource) Edit(c buffalo.Context) error {
	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	// Allocate an empty Benchmark
	benchmark := &models.Benchmark{}

	if err := tx.Find(benchmark, c.Param("benchmark_id")); err != nil {
		return c.Error(404, err)
	}

	return c.Render(200, r.Auto(c, benchmark))
}

// Update changes a Benchmark in the DB. This function is mapped to
// the path PUT /benchmarks/{benchmark_id}
func (v BenchmarksResource) Update(c buffalo.Context) error {
	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	// Allocate an empty Benchmark
	benchmark := &models.Benchmark{}

	if err := tx.Find(benchmark, c.Param("benchmark_id")); err != nil {
		return c.Error(404, err)
	}

	// Bind Benchmark to the html form elements
	if err := c.Bind(benchmark); err != nil {
		return errors.WithStack(err)
	}

	verrs, err := tx.ValidateAndUpdate(benchmark)
	if err != nil {
		return errors.WithStack(err)
	}

	if verrs.HasAny() {
		// Make the errors available inside the html template
		c.Set("errors", verrs)

		// Render again the edit.html template that the user can
		// correct the input.
		return c.Render(422, r.Auto(c, benchmark))
	}

	// If there are no errors set a success message
	c.Flash().Add("success", "Benchmark was updated successfully")

	// and redirect to the benchmarks index page
	return c.Render(200, r.Auto(c, benchmark))
}

// Destroy deletes a Benchmark from the DB. This function is mapped
// to the path DELETE /benchmarks/{benchmark_id}
func (v BenchmarksResource) Destroy(c buffalo.Context) error {
	// Get the DB connection from the context
	tx, ok := c.Value("tx").(*pop.Connection)
	if !ok {
		return errors.WithStack(errors.New("no transaction found"))
	}

	// Allocate an empty Benchmark
	benchmark := &models.Benchmark{}

	// To find the Benchmark the parameter benchmark_id is used.
	if err := tx.Find(benchmark, c.Param("benchmark_id")); err != nil {
		return c.Error(404, err)
	}

	if err := tx.Destroy(benchmark); err != nil {
		return errors.WithStack(err)
	}

	// If there are no errors set a flash message
	c.Flash().Add("success", "Benchmark was destroyed successfully")

	// Redirect to the benchmarks index page
	return c.Render(200, r.Auto(c, benchmark))
}
